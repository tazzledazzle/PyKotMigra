# Catalog showcase

This demo exists so every catalog row has an explicit **Python** module to point at. **MVP** Kotlin references remain [Status Hub](../status-hub/README.md) and [Order API](../order-api/README.md). **Catalog v2** samples (background tasks, API-key auth) are mirrored in Status Hub’s paired Kotlin (`POST /jobs`, `GET /secure/ping`).

- Python service: [`python/README.md`](python/README.md)
